image: node:22

pipelines:
  default:
    - step:
        name: Install and build
        caches:
          - node
        script:
          - npm ci
          - npm run build
  branches:
    main:
      - step:
          name: Install and build
          caches:
            - node
          script:
            - npm ci
            - npm run build
